# 10-input.conf — keyboard and pointer device defaults

input type:keyboard {
    xkb_layout  us
    repeat_delay 300
    repeat_rate  35
}

input type:touchpad {
    tap               enabled
    dwt               enabled
    natural_scroll    enabled
    middle_emulation  enabled
}

seat * hide_cursor 5000
